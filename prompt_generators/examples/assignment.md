Implement the function `average(nums: list[float]) -> float` that returns the
average of `nums`. If `nums` is empty, return `0.0`. Do not use external libraries.
Add a docstring and use clear, descriptive names.
