# Configuration for JADE Independent Worker

# Main server URL
JADE_SERVER_URL=http://localhost:8000

# Redis configuration
REDIS_URL=redis://localhost:6379

# Worker configuration
WORKER_NAME=jade-worker-1
WORKER_TIMEOUT=30m

# API timeout (seconds)
API_TIMEOUT=30

# Logging level (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO
