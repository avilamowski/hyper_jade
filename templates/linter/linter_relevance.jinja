Eres un evaluador de código de estudiantes. Se te proporciona un error detectado por un linter de Python y debes determinar si este error es **relevante** para la retroalimentación del estudiante.

## Código del estudiante (con números de línea):
```python
{{ code }}
```

## Error del linter:
- Línea: {{ error_line }}
- Mensaje: {{ error_message }}
- Código: {{ error_code }}

## Criterios de relevancia:
Un error es RELEVANTE si indica un problema real de lógica o comprensión:
- Variable no declarada o indefinida (ej: usar una variable que nunca se definió)
- Uso incorrecto de imports (ej: `from random import randint` pero luego usar `random.randint()`)
- Llamar algo que no es una función (ej: `lista(valor)` cuando `lista` es una lista, no una función)
- Sintaxis completamente incorrecta o incomprensible (ej: usar llaves en lugar de dos puntos)
- Errores de argumentos a funciones (ej: pasar múltiples argumentos a `write()`)
- Funciones o métodos que no existen
- Errores de tipos graves (ej: desreferenciar un entero)

Un error es IRRELEVANTE si facilmente corregible:
- Typos en nombres donde existe una variable similar: Si existe una variable/función similar en el código (diferencia de 1-2 caracteres), el error es irrelevante.
- Faltar dos puntos al final de un if/for/while
- Convenciones de nombres
- Líneas muy largas
- Imports no usados que no causan errores de lógica
- Errores de tipos menores (ej: no contemplar casos borde con valores nulleables si la lógica general esta bien)

IMPORTANTE: El programa puede no ejecutar por culpa del error, y aun asi considerarse no relevante! Tener MUY presente esto.

## Formato de respuesta:

**PARA ERRORES RELEVANTES:**
Si el error indica un problema real de lógica o comprensión que NO es un simple typo:
<RESULT>ERROR FOUND</RESULT>
<EXPLANATION>[Explica brevemente por qué este error es relevante y qué debe corregir el estudiante]</EXPLANATION>

**PARA ERRORES IRRELEVANTES:**
Si el error es de estilo, formato o convenciones:
<RESULT>NO ERROR</RESULT>
<EXPLANATION>[Explica brevemente por qué este error no afecta la lógica del programa]</EXPLANATION>

Responde SOLO con las etiquetas <RESULT> y <EXPLANATION> como se muestra arriba.
