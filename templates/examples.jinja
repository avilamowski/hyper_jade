You are a programming teacher that is showing students how to do this assignment requirement:
[{{ requirement }}]

{% if assignment_description %}
Contexto del trabajo práctico:
{{ assignment_description }}
{% endif %}

{% if classes_summary %}
Resumen de las clases teóricas:
{{ classes_summary }}
{% endif %}

Your task is to provide {{correct_example_quantity}} examples of python code fragments that correctly implement this requirements{% if erroneous_example_quantity > 0 %} and {{erroneous_example_quantity}} examples of python code fragments that do not implement this requirement{% endif %}.
Just provide the relevant code fragments, not the full program.

You MUST use the following XML structure for your output:

<correct>
    <example>
        <description>Brief description of what makes this correct</description>
        <code>
raw_input = input("Enter x1: ")
try:
    x1 = float(raw_input)
except ValueError:
    print("Invalid input")
        </code>
    </example>
    
    <example>
        <description>Brief description of what makes this correct</description>
        <code>
raw_input = input()
if not raw_input.replace('.', '', 1).isdigit():
    print("Invalid input")
else:
    x1 = float(raw_input)
        </code>
    </example>
</correct>

<erroneous>
    <example>
        <description>Brief description of what's wrong</description>
        <code>
raw_input = input("Enter x1: ")
x1 = float(raw_input)
        </code>
    </example>
    
    <example>
        <description>Brief description of what's wrong</description>
        <code>
x1 = input()
        </code>
    </example>
</erroneous>

IMPORTANT: 
- Use EXACTLY the XML tags shown: <correct>, <erroneous>, <example>, <description>, <code>
- Generate {{correct_example_quantity}} examples in the <correct> section{% if erroneous_example_quantity > 0 %} and {{erroneous_example_quantity}} examples in the <erroneous> section{% endif %}
- Keep code fragments concise and focused on the requirement
- **CRITICAL:** The generated examples MUST strictly adhere to the requirement. It does NOT matter if the code fragment seems nonsensical or incomplete out of context. The ONLY goal is to satisfy (or violate, for erroneous examples) the specific requirement provided. Do not add extra logic or unrelated code just to make it "make sense" as a standalone program.

Remember that the requirement is:
[{{ requirement }}]